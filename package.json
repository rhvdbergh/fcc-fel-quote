{
  "name": "fcc-fel-quote",
  "version": "1.0.0",
  "description": "freeCodeCamp Front End Libraries Project 1 - Quotation Generator",
  "main": "index.js",
  "scripts": {
    "develop": "parcel src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url /fcc-fel-quote/",
    "deploy": "gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ronald van der Bergh",
  "license": "MIT",
  "dependencies": {
    "react": "^16.12.0",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "gh-pages": "^2.1.1",
    "parcel": "^2.8.2"
  }
}
